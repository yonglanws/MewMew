# 表情包管理页优化计划

## 目标

优化表情包管理页，使其与应用现有页面在视觉层级、色彩、间距、组件形态和交互反馈上保持一致，同时不改变现有数据能力。

## 阶段

- [completed] 1. 盘点现有页面、主题和表情包数据/交互
- [completed] 2. 完成设计方案并取得用户确认
- [completed] 3. 先补充/调整可验证的 UI 行为测试
- [completed] 4. 实现页面视觉与交互优化
- [completed] 5. 运行格式化、静态分析、测试并复核改动
- [completed] 6. 确认后续需求设计与实现范围
- [completed] 7. 补充后续需求的失败测试
- [completed] 8. 实现组名编辑、缩略图和操作入口调整
- [completed] 9. 完成格式化、静态分析、测试和合并

## 约束与决策

- 延续现有 Flutter、Material 3 和项目主题，不引入新的 UI 框架。
- 默认保持现有功能范围，重点改善信息架构和视觉一致性。
- 在用户确认设计前不修改生产代码。
- 后续需求不新增文件夹封面字段；文件夹缩略图从其子表情包图片动态派生。
- 表情包名称保留旧数据兼容，但新导入流程不再要求用户输入或展示名称。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 规划技能路径最初定位错误 | 1 | 改用实际的 `planning-with-files/skills/planning-with-files/SKILL.md` 路径 |
| `flutter test` 找不到 `test` 目录 | 1 | 记录为基线限制，先按 TDD 创建测试目录 |
| 第二个 widget 测试使用了不可 const 的 `DateTime` | 1 | 移除测试页面构造处的 `const`，重新运行后得到预期的空状态断言失败 |
| 空状态测试文案少了中文句号 | 1 | 对齐实际 UI 文案后测试通过 |
| `_StickerEntry.subtitle` 类型迁移遗漏 `Text(...)` | 1 | 改为直接传递 Widget，测试重新通过 |
| 一次 apply_patch 使用了错误的 worktree 路径和空 hunk | 1 | 改用正确的绝对路径和具体替换内容 |
| 后续测试使用了不可 const 的 `DateTime` | 1 | 移除测试页面构造处的 `const`，重新运行到预期行为断言失败 |
| 编辑组名弹窗在路由退出动画期间释放控制器 | 1 | 改为由弹窗 StatefulWidget 管理控制器，并补跑跟进测试 |
