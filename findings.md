# 现状与调研发现

## 当前阶段

项目上下文盘点进行中。

## 发现

- 项目是 Flutter 应用，页面集中在 `lib/pages/`，主题位于 `lib/theme/app_theme.dart`。
- 目标页面为 `lib/pages/sticker_management_page.dart`。
- 相关存储逻辑位于 `lib/services/sticker_storage_service.dart`。
- 当前仓库没有既有的任务规划文件。
- 工作区在本次任务开始前没有显示未提交改动。
- 现有设置页截图显示了明确的视觉基线：浅薰衣草色背景、紫色 section 标题、同色系大圆角分组容器、左侧柔和色块图标、标题/副标题两级信息和右侧箭头。
- 目标页当前使用了 `SliverAppBar.large`，但其内部资源/绑定列表仍大量采用独立 `Card`，视觉上更接近普通列表，和设置页的分组容器风格不完全一致。
- 目标页存在多级管理结构：表情包组 → 情绪文件夹 → 表情包，以及人格绑定；优化需要保持层级清晰，避免把所有操作堆在首屏。
- 当前目标页的表情预览使用 `GridView` 与 `Image.file`，删除依赖长按，导入依赖右上角按钮；这些交互可保留，但需要增加更显式的操作提示/入口。

## 待确认

- 目标页面当前的布局层级、空状态和操作入口。
- 其他页面可复用的标题栏、卡片、按钮和表单模式。
- 是否已有针对目标页的测试或可提取的纯逻辑行为。

## 已确认方案

- 用户选择“管理中枢”方向：以设置页风格为骨架，增加资源概览与更清晰的层级入口，不扩展数据能力。

## 后续需求决策

- 选择方案 1：只改展示层和现有状态调用，不增加 `StickerFolder` 封面字段或数据迁移。
- 文件夹缩略图取该文件夹内第一张 `StickerItem.filePath`；文件不存在或没有表情包时回退 `Icons.folder_outlined`。
- `StickerItem.name` 保留以兼容历史存储，但导入流程使用图片文件名作为内部兼容值，不再弹出命名表单，也不在网格卡片中显示名称。
- 组列表行主标题为组名，副标题固定为“表情包组”，数量信息放到摘要/次级信息中，保持列表可扫描性。
